@model IEnumerable<CSharp5Lap45.Data.EF.Entity.FClass>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameClass)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameClass)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdClass">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdClass">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdClass">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
